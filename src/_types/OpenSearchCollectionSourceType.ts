interface OpenSearchCollectionSourceType {
  id: string;
  symbol: string;
  name: string;
  description: string;
  family?: string;
  elementCount: number;
  totalVolume: number;
  floorPrice: number;
}

export default OpenSearchCollectionSourceType;
