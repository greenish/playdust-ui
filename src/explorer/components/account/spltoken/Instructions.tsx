import { PublicKey } from '@solana/web3.js'

interface InstructionsProps {
  pubkey: PublicKey
}

export const Instructions = ({ pubkey }: InstructionsProps) => {
  return <div>Not implemented</div>
}
